@page "/stores"
@inject NavigationManager NavigationManager

@*
    Redirection vers Sources (bloc 4c). Voir Books.razor pour le raisonnement ;
    /stores/{id} reste une page de détail à part entière.
*@

<PageTitle>Magasins - Mes Recettes</PageTitle>

@code {
    protected override void OnInitialized() =>
        NavigationManager.NavigateTo("/sources?type=store", replace: true);
}
